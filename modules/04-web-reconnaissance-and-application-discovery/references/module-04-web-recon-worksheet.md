# Module 04 Web Recon Worksheet

---

> **📝 Worksheet Purpose**
>
> Use this worksheet during passive and active web recon so naming, routes, technologies, and trust boundaries are captured intentionally instead of scattered across terminal history.

## Fill-In Structure

```text
Assessment context:
Primary target URL:
Known IP / host:
Known hostnames or subdomains:
Scope notes or exclusions:

Passive recon clues:
- cert names:
- prior scan clues:
- public references:
- likely role names:

Redirect behavior:
HTTP vs HTTPS notes:
Observed titles / visible product names:
Technology clues:

Public routes found:
Auth routes found:
Admin or ops routes found:
API or docs routes found:
Interesting parameters or form fields:
File upload / export / import clues:

Observation:
Inference:
What still needs validation:
Most important flows to capture in a proxy next:
```

---

## Design Notes

This worksheet is meant to pair naturally with the route map template and should produce inputs for Lesson 4.4 and Module 05.
